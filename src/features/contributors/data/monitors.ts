import type { MaterialContributor } from "../contributors.types";

// Curated by hand: monitoria isn't tied to a commit, so it can't be derived from git.
export const MONITORS: MaterialContributor[] = [
  {
    name: "Eduardo Melo",
    role: "Monitor(a) de MAC",
    githubUrl: "https://github.com/eduardoksm13-beep",
  },
];
